[credential]
    helper = manager

[user]
    name = YDH
    email = swimming.polar@gmail.com

[alias]
    ignore = update-index --assume-unchanged
    unignore = update-index --no-assume-unchanged
    ignored = !git ls-files -v | grep "^[[:lower:]]"
[core]
	pager = git-split-diffs --color | less -RFX
